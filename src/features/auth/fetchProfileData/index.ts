import {
    profileDataRequested,
    profileDataFailed,
    profileDataReceived,
} from './model';

export const FetchProfileData = {
    model: {
        profileDataRequested,
        profileDataReceived,
        profileDataFailed,
    },
};
