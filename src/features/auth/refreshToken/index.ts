import {
    tokenRefreshRequested,
    updateTokenFail,
    updateTokenSuccess,
} from './model';

export const RefreshToken = {
    tokenRefreshRequested,
    updateTokenSuccess,
    updateTokenFail,
};
