import {
    forceRefreshRequested,
    startTokenRefreshWatcher,
    updateTokenFail,
    updateTokenSuccess,
} from './model';

export const RefreshToken = {
    startTokenRefreshWatcher,
    forceRefreshRequested,
    updateTokenSuccess,
    updateTokenFail,
};
