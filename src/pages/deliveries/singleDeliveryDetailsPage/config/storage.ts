import { sharedConfigConstants } from '@/shared/config';

const { APP_NAME } = sharedConfigConstants;

export const PAGE_SINGLE_DELIVERY_OFFLINE_STORAGE_KEY = `${APP_NAME.toUpperCase()}_PAGE_SINGLE_DELIVERY_OFFLINE_STORAGE_KEY`;
