import './init';
import { init, reset, setOffline } from './model/model';

export const widgetSelectAvatarModel = {
    init,
    setOffline,
    reset,
};
export * as widgetSelectAvatarUi from './ui';
